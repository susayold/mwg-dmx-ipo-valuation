# Curated output area

Run `mwg-dmx-analytics build ... --output-dir data/curated/<run-name>` to
materialize a reproducible analytics run here. Generated databases and result
artifacts are ignored by Git; the source files and assumptions used to create
them should be versioned instead.
